/*
    Const example for DanBooru.
    Use this as a guideline (NOT A COPY + PASTE) of how the provider should look.
    Licensed as GPL-3.0, see the branch LICENSE for full licensing.
*/

namespace DanBooruProvider;

// This class just contains re-used values throughout the provider so it's in one centralized location.

internal static class Const {
    public const string DateTimeFormat = "yyyy-MM-dd'T'HH:mm:ss.fffK";
    public const int PostsPerPage = 200;
}
